<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, show_path(task) %> => <%= task.completed ? "Done !" : "To do !" %></li>
  <% end %>
</ul>
<p>
  <%=  link_to "Créer une nouvelle tâche", new_path %>
</p>
